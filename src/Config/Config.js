var cf = cf || {};

cf.SCALE = 1;

cf.bgSCALE = cf.SCALE*2;

cf.squareSize = 50*cf.SCALE;

cf.tileSize = {
    height: 57*cf.SCALE,
    width: 76*cf.SCALE
};

cf.offsetX = 40*cf.SCALE;

cf.buildingScale = cf.SCALE;

cf.tileLocation = [];